/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "assets.unileversolutions.com",
      "soyummyrecipes.com",
      "www.topwisata.info"
    ],
  },
};

export default nextConfig;